package pit.ambiguous.mutation.name;

public class SomeClass {
    public boolean someMethod(Object object) {
        if (object != null) {
            return true;
        }
        return false;
    }
}
